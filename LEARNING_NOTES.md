# 学习笔记的维护与发布

总目录为 `/studio/notes/`，工作台顶端有独立的「学习笔记」卡片。原来的「AI 笔记」仍指向六层知识图谱。

## 放入一篇笔记

1. 将适合公开的 HTML 保存为 `public/studio/notes/<固定名称>/index.html`。保留来源归属和日期；原始录音、私人逐字稿、未确认的个人资料不随站点上传。
2. 在 `lib/learning-notes.json` 的 `items` 中登记唯一 `id`、类别 `kind`、固定 `href`、标题、说明、日期、最后更新日期、主题标签和简短更新说明。该 JSON 是笔记目录的主要维护位置；`lib/content.ts` 统一导出。
3. 类别为 `offline`（线下课）、`workshop`、`mba`、`podcast`。目前空分类保持为空，不用占位条目制造已发布内容。
4. `pnpm build` 会先生成版本清单，再执行网站构建。`pnpm dev` 和 `pnpm export` 也包含准备步骤。

## 更新已有笔记

继续修改同一篇 HTML，同步 `updatedAt` 和 `updateNote`，然后构建、检查、提交和发布。不要给同一篇笔记不断换网址。

`scripts/prepare-learning-notes.mjs` 根据真实正文和目录内容计算版本，生成 `lib/learning-note-versions.json` 和 `public/studio/notes/updates.json`，并为已登记的阅读页接入更新提示。请运行脚本生成这些文件，不要手动写版本哈希。

网页可见时约每 60 秒检查一次已发布版本；从后台切回也会检查。发现新版本时提示读者刷新，保留当前阅读位置。它不读取飞书，也不会自动把本地未发布修改暴露到网上。

## 上线链路

沿用本仓库现有主分支部署。一次发布完成后，同一个网址显示新版。只有发布成功且版本清单可读，读者才会看到更新提示；不是多人实时共同编辑。

更新后核对：目录分类与空状态；卡片到阅读页及返回链接；正文来源；版本脚本幂等；部署后页面与版本清单。
